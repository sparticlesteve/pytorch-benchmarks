trainer: gan
output_dir: results/dcgan

data_config:
    name: random
    n_train: 65536
    n_valid: 65536
    input_shape: [3, 64, 64]
    target_shape: null
    batch_size: 128
    use_dist_sampler_train: false
    use_dist_sampler_valid: false

model_config:
    name: dcgan
    noise_dim: 64
    n_channels: 3

train_config:
    n_epochs: 4
